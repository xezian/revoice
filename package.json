{
  "name": "revoice",
  "version": "1.0.0",
  "description": "A game that involves reversing vocal audio clips",
  "main": "index.js",
  "repository": "git@github.com:xezian/revoice.git",
  "author": "xezian <xezian@icloud.com>",
  "license": "MIT",
  "scripts": {
    "install": "cd client && yarn",
    "dev": "concurrently \"cd client && yarn start\" \"yarn start\"",
    "start": "node server",
    "lint": "eslint .",
    "test": "yarn test:server; yarn test:client",
    "test:client": "cd client; yarn test;",
    "test:server": "mocha --timeout 10000 --exit",
    "heroku-postbuild": "cd client && yarn run build"
  },
  "dependencies": {
    "body-parser": "^1.18.2",
    "chai-http": "^4.0.0",
    "express": "^4.16.3",
    "mongoose": "^5.1.0",
    "web-audio-api": "^0.2.2"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "concurrently": "^3.5.1",
    "mocha": "^5.1.1"
  }
}
